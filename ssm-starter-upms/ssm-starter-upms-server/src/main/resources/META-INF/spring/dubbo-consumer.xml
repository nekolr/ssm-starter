<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="
  http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
  http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 定义应用的名称 -->
    <dubbo:application name="ssm-starter-consumer"/>

    <!-- 使用广播机制进行服务注册与发现，注意需要跟 ssm-starter-provider 使用相同的广播地址 -->
    <dubbo:registry address="multicast://224.5.6.7:1234"/>

    <!-- 定义 RPC 传输协议 -->
    <dubbo:protocol name="dubbo"/>

    <!-- 引用 ssm-starter-provider 暴露出来的 UserService 服务 -->
    <dubbo:reference id="userServiceReference"
                     interface="com.nekolr.upms.api.rpc.UserService"
                     version="1.0"/>

    <!-- 引用 ssm-starter-provider 暴露出来的 AccountService 服务 -->
    <dubbo:reference id="helloServiceReference"
                     interface="com.nekolr.upms.api.rpc.AccountService"
                     version="1.0"/>

</beans>
